spring:
  application:
    name: api-delivery
  data:
    mongodb:
      uri: mongodb://localhost:27017/delivery

management:
  endpoint:
    metrics:
      enabled: true
  endpoints:
    web:
      base-path: /management
      exposure:
        include: info, health, metrics

server:
  servlet:
    context-path: /asdf
